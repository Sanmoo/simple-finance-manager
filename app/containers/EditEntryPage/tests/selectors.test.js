// import { selectEditEntryPageDomain } from '../selectors';

describe('selectEditEntryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
