/*
 * ListEntriesPage Messages
 *
 * This contains all the text for the ListEntriesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ListEntriesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ListEntriesPage container!',
  },
});
